<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Monthly Occupancy Report - Sample Data</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f3f4f6;
            padding: 1.5rem;
        }
        .container {
            background-color: white;
            padding: 2rem;
            border-radius: 1rem;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            max-width: 64rem;
            margin-left: auto;
            margin-right: auto;
        }
        .header {
            text-align: center;
            margin-bottom: 2rem;
            border-bottom: 3px solid #ef4444;
            padding-bottom: 1.5rem;
        }
        .header-content {
            display: flex;
            align-items: center;
            justify-content: flex-end;
            margin-top: 0.5rem;
        }
        .header-legend {
            background-color: #ef4444;
            color: white;
            padding: 0.25rem 0.5rem;
            border-radius: 0.375rem;
            font-size: 0.875rem;
        }
        .title {
            background-color: #ef4444;
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 0.5rem;
            display: inline-block;
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        .chart-container {
            margin: 2rem 0;
            position: relative;
            height: 450px;
            border: 1px solid #d1d5db;
            background-color: #f9fafb;
            padding: 1.5rem;
            border-radius: 0.5rem;
        }
        .chart-wrapper {
            position: absolute;
            top: 2rem;
            left: 5rem;
            right: 2rem;
            bottom: 5rem;
            border-left: 2px solid #1f2937;
            border-bottom: 2px solid #1f2937;
        }
        .chart {
            display: flex;
            align-items: flex-end;
            height: 100%;
            padding: 0 0.5rem;
            gap: 3px;
        }
        .bar {
            background-color: #ef4444;
            min-height: 2px;
            flex: 1;
            position: relative;
            border-radius: 0.25rem 0.25rem 0 0;
            border: 1px solid #b91c1c;
        }
        .bar-value {
            position: absolute;
            top: -1.25rem;
            left: 50%;
            transform: translateX(-50%);
            font-size: 0.65rem;
            color: #1f2937;
            font-weight: 700;
            white-space: nowrap;
        }
        .x-axis {
            position: absolute;
            bottom: -2rem;
            left: 0;
            right: 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .x-label {
            font-size: 0.75rem;
            color: #6b7280;
            text-align: center;
            flex: 1;
        }
        .y-axis {
            position: absolute;
            left: 1.5rem;
            top: 2rem;
            bottom: 5rem;
            height: calc(100% - 7rem);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            align-items: flex-end;
            z-index: 2;
        }
        .y-label {
            font-size: 0.75rem;
            color: #6b7280;
            margin-right: 3px;
            line-height: 1;
            transform: translateY(50%);
        }
        .y-label-zero {
            position: relative;
            margin-right: 3px;
            display: flex;
            align-items: center;
            line-height: 1;
            transform: translateY(50%);
        }
        .red-flag {
            width: 8px;
            height: 8px;
            background-color: #ef4444;
            border-radius: 50%;
            margin-right: 0.25rem;
        }
        .grid-lines {
            position: absolute;
            left: 0;
            right: 0;
            top: 0;
            height: 100%;
            display: flex;
            flex-direction: column-reverse;
            justify-content: space-between;
            z-index: 1;
            padding-bottom: 0.2rem;
        }
        .grid-line {
            border-top: 1px dashed #e5e7eb;
            width: 100%;
        }
        .legend-container {
            display: flex;
            justify-content: space-around;
            margin-top: 1.5rem;
            padding: 1rem;
            background-color: #f1f5f9;
            border-radius: 0.5rem;
        }
        .legend-item {
            text-align: center;
        }
        .legend-value {
            font-size: 1.5rem;
            font-weight: 700;
            color: #1f2937;
        }
        .legend-label {
            font-size: 0.8rem;
            color: #6b7280;
            margin-top: 0.25rem;
        }
        .footer {
            margin-top: 2rem;
            text-align: center;
            font-size: 0.7rem;
            color: #9ca3af;
            border-top: 1px solid #e5e7eb;
            padding-top: 1rem;
            display: flex;
            justify-content: space-between;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="title" id="reportTitle"></h1>
            <div class="header-content">
                <div class="header-legend">
                    ■ Daily Reservations
                </div>
            </div>
        </div>
        
        <div class="chart-container">
            <div class="y-axis" id="yAxis"></div>
            <div class="chart-wrapper">
                <div class="grid-lines" id="gridLines"></div>
                <div class="chart" id="chart"></div>
                <div class="x-axis" id="xAxis"></div>
            </div>
        </div>
        
        <div class="legend-container">
            <div class="legend-item">
                <div class="legend-value" id="avgReservations"></div>
                <div class="legend-label">Average Daily Reservations</div>
            </div>
            <div class="legend-item">
                <div class="legend-value" id="maxReservations"></div>
                <div class="legend-label">Peak Daily Reservations</div>
            </div>
            <div class="legend-item">
                <div class="legend-value" id="totalReservations"></div>
                <div class="legend-label">Total Reservations</div>
            </div>
        </div>
        
        <div class="footer">
            <p>Printed On: <span id="printedDate"></span></p>
            <p>Printed By: <span id="printedBy"></span></p>
            <p>Page 1 of 1</p>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const chart = document.getElementById('chart');
            const xAxis = document.getElementById('xAxis');
            const yAxis = document.getElementById('yAxis');
            const gridLines = document.getElementById('gridLines');
            const reportTitle = document.getElementById('reportTitle');
            const avgReservationsEl = document.getElementById('avgReservations');
            const maxReservationsEl = document.getElementById('maxReservations');
            const totalReservationsEl = document.getElementById('totalReservations');
            const printedDateEl = document.getElementById('printedDate');
            const printedByEl = document.getElementById('printedBy');

            // --- Sample Data Generation ---
            const monthName = 'September';
            const printedBy = 'John Doe';
            const numDays = 30;
            const reservations = [];
            let totalReservations = 0;

            for (let i = 1; i <= numDays; i++) {
                const count = Math.floor(Math.random() * 50) + 10; // Random number between 10 and 59
                reservations.push({ day: i, count: count });
                totalReservations += count;
            }

            const maxReservations = Math.max(...reservations.map(r => r.count));
            const avgReservations = Math.round(totalReservations / numDays);
            const maxChartHeight = 350;
            const yAxisMax = 60; // Set a fixed max for the y-axis to match the style

            // --- HTML Injection ---
            reportTitle.textContent = `Monthly Occupancy Report - ${monthName}`;
            
            // Y-axis labels and grid lines
            const yAxisValues = [yAxisMax, Math.ceil(yAxisMax * 0.75), Math.ceil(yAxisMax * 0.5), Math.ceil(yAxisMax * 0.25), 0];
            yAxis.innerHTML = yAxisValues.map(val => {
                if (val === 0) {
                    return `<div class="y-label-zero"><span class="red-flag"></span>${val}</div>`;
                }
                return `<div class="y-label">${val}</div>`;
            }).join('');
            
            gridLines.innerHTML = yAxisValues.slice(0, -1).map(() => `<div class="grid-line"></div>`).join('');

            // Chart bars and X-axis labels
            let chartHtml = '';
            let xAxisHtml = '';
            reservations.forEach(data => {
                const height = (data.count / yAxisMax) * maxChartHeight;
                chartHtml += `
                    <div class="bar" style="height: ${height}px;">
                        <div class="bar-value">${data.count}</div>
                    </div>
                `;
                xAxisHtml += `<div class="x-label">${data.day}</div>`;
            });

            chart.innerHTML = chartHtml;
            xAxis.innerHTML = xAxisHtml;

            // Summary data
            avgReservationsEl.textContent = avgReservations;
            maxReservationsEl.textContent = maxReservations;
            totalReservationsEl.textContent = totalReservations;
            
            // Footer data
            const now = new Date();
            const formattedDate = `${now.getDate()}/${now.getMonth() + 1}/${now.getFullYear()} ${now.getHours()}:${now.getMinutes()}`;
            printedDateEl.textContent = formattedDate;
            printedByEl.textContent = printedBy;
        });
    </script>
</body>
</html>
