<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ hotelName }} - Daily Receipt Summary</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            font-size: 11px;
            padding: 10px;
            background-color: white;
        }

        .report-container {
            padding: 15px;
            background-color: white;
        }

        .report-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
            padding-bottom: 5px;
        }

        .report-title {
            border: 2px solid #c00;
            background: white;
            color: #c00;
            padding: 5px 15px;
            font-size: 13px;
            font-weight: bold;
            border-radius: 3px;
        }

        .date-range {
            font-size: 11px;
            margin-bottom: 15px;
        }

        .date-range strong {
            font-weight: bold;
        }

        /* User sections */
        .user-section {
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 10px;
            margin-bottom: 5px;
        }

        table th {
            background-color: #f5f5f5;
            border-top: 1px solid #333;
            border-bottom: 1px solid #333;
            padding: 5px 8px;
            text-align: left;
            font-weight: bold;
            font-size: 10px;
        }

        table th.text-right,
        table td.text-right {
            text-align: right;
        }

        table th.text-center,
        table td.text-center {
            text-align: center;
        }

        table td {
            padding: 4px 8px;
            font-size: 10px;
            border: none;
        }

        /* Ligne avec le nom d'utilisateur */
        .user-name-row td {
            padding: 6px 8px;
            font-weight: bold;
            border: none;
        }

        /* Lignes des méthodes de paiement - bordures pointillées sur colonnes spécifiques */
        .payment-method-row td {
            border: none;
        }

        /* Bordures pointillées sur Tran. Count, Void Count et Total uniquement */
        .payment-method-row td:nth-child(2),
        .payment-method-row td:nth-child(4),
        .payment-method-row td:nth-child(6) {
            border-bottom: 1px dotted #ccc;
        }

        /* Ligne User Total - bordures en pointillés sur colonnes spécifiques */
        .user-total-row {
            font-weight: bold;
            background-color: #f9f9f9;
        }

        .user-total-row td {
            border: none;
        }

        /* Bordures pointillées sur Tran. Count, Void Count et Total uniquement */
        .user-total-row td:nth-child(2),
        .user-total-row td:nth-child(4),
        .user-total-row td:nth-child(6) {
            border-top: 1px dotted #999;
            border-bottom: 1px dotted #999;
        }

        .separator-line {
            border-top: 1px solid #999;
            margin: 10px 0;
        }

        /* Grand Total */
        .grand-total-row {
            font-weight: bold;
            background-color: #f0f0f0;
        }

        .grand-total-row td {
            padding: 6px 8px;
            border: none;
        }

        /* Bordures pointillées sur Tran. Count, Void Count et Total uniquement */
        .grand-total-row td:nth-child(2),
        .grand-total-row td:nth-child(4),
        .grand-total-row td:nth-child(6) {
            border-top: 1px dotted #999;
            border-bottom: 1px dotted #999;
        }

        /* Summary section */
        .summary-section {
            margin-top: 20px;
            padding-top: 15px;
            border-top: 2px solid #000;
        }

        .summary-title {
            font-weight: bold;
            font-size: 12px;
            margin-bottom: 10px;
            text-align: center;
        }

        .summary-tables {
            display: flex;
            gap: 20px;
            justify-content: space-between;
        }

        .summary-table-container {
            flex: 1;
        }

        .summary-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 10px;
            border: none;
        }

        .summary-table th {
            background-color: #f5f5f5;
            border-top: 1px solid #333;
            border-bottom: 1px solid #333;
            padding: 5px 8px;
            text-align: left;
            font-weight: bold;
        }

        .summary-table td {
            border: none;
            padding: 4px 8px;
        }

        /* Bordures pointillées pour les lignes normales du summary */
        .summary-table tbody tr:not(.summary-grand-total) td {
            border-bottom: 1px dotted #ccc;
        }

        .summary-grand-total {
            font-weight: bold;
            background-color: #f0f0f0;
        }

        .summary-grand-total td {
            border-top: 1px dotted #999;
            border-bottom: 1px dotted #999;
        }

        .amount-column {
            text-align: right;
            font-family: 'Courier New', monospace;
        }

        .no-data {
            text-align: center;
            padding: 20px;
            color: #666;
            font-style: italic;
        }
    </style>
</head>
<body>
    <div class="report-container">

        <!-- User Sections -->
        @each(user in data.userSummaries)
            <div class="user-section">
                <table>
                    <thead>
                        <tr>
                            <th style="width: 25%;">Pay Method</th>
                            <th class="text-center" style="width: 15%;">Tran. Count</th>
                            <th class="text-right" style="width: 20%;">Amount</th>
                            <th class="text-center" style="width: 15%;">Void Count</th>
                            <th class="text-right" style="width: 20%;">Void Amount</th>
                            <th class="text-right" style="width: 20%;">Total ({{ currency }})</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr class="user-name-row">
                            <td colspan="6">User: {{ user.userName }}</td>
                        </tr>
                        @each(method in user.paymentMethods)
                            <tr class="payment-method-row">
                                <td>{{ method.methodName }}</td>
                                <td class="text-center">{{ method.totalTransactions }}</td>
                                <td class="text-right amount-column">{{ formatCurrency(method.amount) }}</td>
                                <td class="text-center">{{ method.totalVoid }}</td>
                                <td class="text-right amount-column">{{ formatCurrency(method.voidAmount) }}</td>
                                <td class="text-right amount-column">{{ formatCurrency(method.total) }}</td>
                            </tr>
                        @end
                        <tr class="user-total-row">
                            <td><strong>User Total</strong></td>
                            <td class="text-center"><strong>{{ user.totalTransactions }}</strong></td>
                            <td class="text-right amount-column"><strong>{{ formatCurrency(user.totalAmount) }}</strong></td>
                            <td class="text-center"><strong>{{ user.totalVoid }}</strong></td>
                            <td class="text-right amount-column"><strong>{{ formatCurrency(user.voidAmount) }}</strong></td>
                            <td class="text-right amount-column"><strong>{{ formatCurrency(user.userTotal) }}</strong></td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div class="separator-line"></div>
        @end

        <!-- Grand Total -->
        <table style="margin-top: 10px;">
            <tbody>
                <tr class="grand-total-row">
                    <td style="width: 25%;"><strong>Grand Total</strong></td>
                    <td class="text-center" style="width: 15%;"><strong>{{ data.grandTotals.totalTransactions }}</strong></td>
                    <td class="text-right amount-column" style="width: 20%;"><strong>{{ formatCurrency(data.grandTotals.totalAmount) }}</strong></td>
                    <td class="text-center" style="width: 15%;"><strong>{{ data.grandTotals.totalVoid }}</strong></td>
                    <td class="text-right amount-column" style="width: 20%;"><strong>{{ formatCurrency(data.grandTotals.voidAmount) }}</strong></td>
                    <td class="text-right amount-column" style="width: 20%;"><strong>{{ formatCurrency(data.grandTotals.netTotal) }}</strong></td>
                </tr>
            </tbody>
        </table>

        <!-- Summary Section -->
        <div class="summary-section">
            <div class="summary-title">Summary</div>
            
            <div class="summary-tables">
                <!-- User Summary -->
                <div class="summary-table-container">
                    <table class="summary-table">
                        <thead>
                            <tr>
                                <th colspan="2" style="text-align: center;">User</th>
                                <th class="text-right">Amount ({{ currency }})</th>
                            </tr>
                        </thead>
                        <tbody>
                            @each(userSum in data.summary.userSummary)
                                <tr>
                                    <td colspan="2">{{ userSum.userName }}</td>
                                    <td class="text-right amount-column">{{ formatCurrency(userSum.netTotal) }}</td>
                                </tr>
                            @end
                            <tr class="summary-grand-total">
                                <td colspan="2"><strong>Grand Total</strong></td>
                                <td class="text-right amount-column"><strong>{{ formatCurrency(data.summary.grandTotalUserSummary.netTotal) }}</strong></td>
                            </tr>
                        </tbody>
                    </table>
                </div>

                <!-- Payment Method Summary -->
                <div class="summary-table-container">
                    <table class="summary-table">
                        <thead>
                            <tr>
                                <th>Pay Method</th>
                                <th class="text-right">Amount ({{ currency }})</th>
                            </tr>
                        </thead>
                        <tbody>
                            @each(methodSum in data.summary.paymentMethodSummary)
                                <tr>
                                    <td>{{ methodSum.methodName }}</td>
                                    <td class="text-right amount-column">{{ formatCurrency(methodSum.total) }}</td>
                                </tr>
                            @end
                            <tr class="summary-grand-total">
                                <td><strong>Grand Total</strong></td>
                                <td class="text-right amount-column"><strong>{{ formatCurrency(data.summary.grandTotalPaymentMethodSummary.netTotal) }}</strong></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</body>
</html>